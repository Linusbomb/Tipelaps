services:
  db:
    image: postgres:16-alpine
    environment:
      POSTGRES_USER: timelaps
      POSTGRES_PASSWORD: timelaps
      POSTGRES_DB: timelaps
    ports:
      - "5432:5432"
    volumes:
      - timelaps_pg:/var/lib/postgresql/data

volumes:
  timelaps_pg:
