# PostgreSQL (lokalt: se docker-compose.yml)
DATABASE_URL="postgresql://timelaps:timelaps@localhost:5432/timelaps"

JWT_SECRET="byt-mig-i-produktion"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Valfritt – e-post (lösenordsåterställning m.m.)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=
