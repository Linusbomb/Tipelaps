# PostgreSQL (lokalt: se docker-compose.yml)
DATABASE_URL=postgresql://timelaps:timelaps@127.0.0.1:5433/timelaps

JWT_SECRET="byt-mig-i-produktion"
NEXT_PUBLIC_BASE_URL="http://localhost:3000"

# Valfritt – e-post (lösenordsåterställning m.m.)
# SMTP_HOST=
# SMTP_PORT=587
# SMTP_USER=
# SMTP_PASS=
# SMTP_FROM=

# Valfritt – mottagare för säkerhetslarm (brute-force m.m.). Faller tillbaka på SMTP_FROM/SMTP_USER.
# SECURITY_ALERT_EMAIL=
